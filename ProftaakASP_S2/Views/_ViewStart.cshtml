@using Microsoft.AspNetCore.Http
@{
    string currentRole = "";

    if (User.IsInRole("Admin"))
    {
        currentRole = "Admin";
    }
    else if (User.IsInRole("CareRecipient"))
    {
        currentRole = "CareRecipient";
    }
    else if(User.IsInRole("Volunteer"))
    {
        currentRole = "Volunteer";
    }
    else if (User.IsInRole("Professional"))
    {
        currentRole = "Professional";
    }

    switch (currentRole)
    {
        case "CareRecipient":
            Layout = "~/Views/CareRecipient/_Layout.cshtml";

            break;
        case "Volunteer":
            Layout = "~/Views/Volunteer/_Layout.cshtml";
            break;
        case "Professional":
            Layout = "~/Views/Professional/_Layout.cshtml";
            break;
        case "Admin":
            Layout = "~/Views/Admin/_Layout.cshtml";
            break;
        default:
            Layout = "~/Views/Shared/_LayoutHome.cshtml";
            break;
    }
}


