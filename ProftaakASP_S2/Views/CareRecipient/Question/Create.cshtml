@using Models
@model ProftaakASP_S2.Models.QuestionViewModel

@{
    ViewData["Title"] = "View";
}

<h2>Hulpvraag aanmaken</h2>

<div class="row login-div">
    <div class="col-md-4">
        <form style="width: 465px" asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group care-label">
                <label asp-for="Title" class="control-label">Onderwerp</label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-group care-label">
                <label asp-for="Content" class="control-label">Inhoud</label>
                <textarea style="height: 100px" asp-for="Content" class="form-control"></textarea>
                <span asp-validation-for="Content" class="text-danger"></span>
            </div>
            <div class="form-group care-label">
                <label asp-for="Urgency" class="control-label">Urgent</label>
                <input asp-for="Urgency" class="form-control" />
                <span asp-validation-for="Urgency" class="text-danger"></span>
            </div>
            <div class="form-group care-label">
                <label asp-for="CategoryId" class="control-label">Categorie</label>
                <select asp-for="CategoryId" class="form-control">
                    @foreach (Category category in ViewData["Categories"] as IList<Category>)
                    {
                        <option value="
                                    @category.CategoryId">
                            @category.Name
                        </option>
                    }
                </select>
            </div>
            <div class="form-group">
                <input class="btn btn-primary btn-lg btn-block" type="submit" value="Aanmaken" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
